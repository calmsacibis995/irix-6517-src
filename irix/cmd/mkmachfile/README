mkmachfile - create /var/inst/machfile

/var/inst/machfile is a plain text file which contains information to map
hardware to mach tags.  See inst(1M) and swmgr(1M).

The libinst and libsaNetwork DSOs depend on machfile.  Any changes to the
machfile layout will likely effect these DSOs.


