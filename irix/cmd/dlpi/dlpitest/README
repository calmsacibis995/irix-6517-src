First run "setmac" to configure the MAC address of your machine. The
parameters are "old-mac" "new-mac". The default "old-mac" is "0:0:0:0:0:0".

To run the tests, run the script 'runtests'.

The output of each test is stored in the directory "output".
