#
# $Revision: 1.10 $
#
# NOTE: lines beginning with # are comments
#
# This file contains a list of those architectures which are supported as
# diskless clients.  The list is meant to be equivalent with the supported
# architectures chart in the client.dat file.  Although duplicates are OK,
# please do not put them in here.  "inst" uses this with the -C option to
# install all supported architectures.
#
#CPUBOARD/MACH	CPUARCH		GFXBOARD	SUBGR		VIDEO		MODE
#
IP20		R4000		LIGHT		LG1MC 		NULL		32bit
IP20		R4000		EXPRESS		EXPRESS		NULL		32bit
IP22		R4000		EXPRESS		EXPRESS		NULL		32bit
IP22		R4000		NEWPRESS	NEWPRESS	NULL		32bit
IP22		R4000		NEWPORT		NG1		NULL		32bit	
IP22		R4000		MGRAS		MGRAS		NULL		32bit
IP22		R4000		NEWPORT		NG1		VINO		32bit
IP22		R5000		NEWPORT		NEWTON		VINO		32bit
IP28		R10000		EXPRESS		I2_64		NULL		64bit
IP28		R10000		MGRAS		I2_64		NULL		64bit
IP30		R10000		MGRAS		RACER		NULL		64bit
IP32		R5000		CRIME		CRM		VICE		32bit
IP32		R10000		CRIME		CRM		VICE		32bit
