#
# Fundamental constants of the build tree (distinct from source tree).
# This file is installed as $(ROOT)/usr/include/make/defs in the vbuild 
# process, so that when PRODUCT is unset, including $(PRODUCTDEFS) won't fail.
#
SYSTEM	= SVR4
