* deftune.cf -- set tuneables to the default
*
* "$Revision: 1.2 $"

* defining LIBSK prevents us for over-ridding libsc defaults with the libsk
* defaults.  Individual programs can override them without worry, but we
* (libsk) should let them be.  Libsk tuneables are protected by _KERNEL.
*
#define _LIBSK

tune:
