preop("for d in $rbase/usr/lib $rbase/usr/lib32 $rbase/usr/lib64; do if [ -d $d ]; then cd $d; [ -r libpcp_pmda.so -a ! -l libpcp_pmda.so -a ! -f libpcp_pmda.so.1 ] && mv libpcp_pmda.so libpcp_pmda.so.1 && ln -s libpcp_pmda.so.1 libpcp_pmda.so; fi; done; exit 0")
