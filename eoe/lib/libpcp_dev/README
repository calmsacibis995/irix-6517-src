This is a static noship library containing functions pertaining to
licensing or security. It is only distributed in the pcp_root isms (for
IRIX6.4 or earlier) and in the IRIX root ism (for IRIX6.5 or later).
The corresponding header is ../include/pmapi_dev.h (for PCP) and
$(ROOT)/usr/include/pcp/pmapi_dev.h (for IRIX).
