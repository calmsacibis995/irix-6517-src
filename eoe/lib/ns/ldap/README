/*
 * This directory has code that transform some regular name service
 * queries into LDAP queries and return the retrieved information to
 * the application issue the request under the new name service scheme
 * of IRIX
 */
