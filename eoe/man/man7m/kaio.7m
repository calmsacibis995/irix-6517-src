'\"! mmdoc
'\"macro stdmacro
.TH KAIO 7M
.SH NAME
kaio \- kernel asynchronous I/O driver for database servers
.SH DESCRIPTION
The kaio driver is part of a specialized async I/O mechanism for raw
devices.  The device is accessed by libdba.so, which is called from the
aio_*(3) functions in libc. Users should not attempt to use this device
directly.  The interface is likely to change in future releases.
.SH SEE ALSO
aio_sgi_init(3)
