#include <stdio.h>

main (int argc, char *argv[])
{
    fprintf(stderr, "This program (%s) is just a stub.\n", argv[0]);
    fprintf(stderr, "Please refer to release notes and man pages of ESP for more details.\n");
    exit(1);
}
