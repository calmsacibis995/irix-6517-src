This directory contains code for the SEH which is a sub-system of the SEM which
is a modules in the SSS software.

seh_events     - Directory contains the event handling code.

seh_interfaces - Directory contains the code to interface with other modules,
                 DSM and SSDB.

seh_misc       - Miscellaneous code, which did not necessitate the creation
                 of separate directories.

include        - Include files for the SEH.