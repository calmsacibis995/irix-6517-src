This directory contains code for the DSM which is a sub-system of the SEM which
is a modules in the SSS software.

dsm_events     - Directory contains the event handling code.

dsm_interfaces - Directory contains the code to interface with other modules,
                 SEH and SSDB.

dsm_misc       - Miscellaneous code, which did not necessitate the creation
                 of separate directories.

dsm_rules      - Contains the rules parsing code.

dsm_tools      - Standalone executables which are used by the dsm or are part
                 of the dsm sub-system are contained in here.

include        - Include files for the DSM.