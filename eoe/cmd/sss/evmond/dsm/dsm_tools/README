This directory contains code for standalone executables which are required by 
the DSM or needed by users of the DSM.

dsm_wrap.c     - The wrapper executable. If timeout then kill.
rules_check    - This contains the directory for the executable which will
                 parse the rule entered by the user for correctness.
semerrors.c    - Code to print a verbose output of the supplied error code.


