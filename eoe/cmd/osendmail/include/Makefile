# make sendmail configuration file

include	$(ROOT)/usr/include/make/commondefs


# just RCS check-out the header files  (someday, when there are RCS rules)
default install:useful.h userdbm.h

clean:
clobber:
