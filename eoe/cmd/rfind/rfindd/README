This directory, /var/rfindd, is the home of the
rfindd daemon (server) and fsdump.

See related files in:

/etc/rfind.aliases	 -- list of aliases for client
/etc/init.d/rfindd	 -- init script for rfindd (linked to /etc/rc?.d)
/var/spool/cron/crontabs -- rfindd crontab runs runfsdump
/etc/passwd		 -- added entry for id rfindd
/etc/config/rfindd	 -- autoconfig on/off flag for rfindd, fsdump

See the manual pages: rfind(1), rfindd(1M) and fsdump(1M).

Silicon Graphics
6 Aug 91
