mmsbrk2
-------

Another simple test of mmap'ing over the heap.  This one sbrks
a 20 page heap, replaces the whole thing with an mmap'ed file,
then sbrks again to make sure you can still grow more heap space
immediately after the mmap'ed file.
