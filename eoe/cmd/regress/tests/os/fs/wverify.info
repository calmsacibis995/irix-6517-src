

wverify

This tests creates a directory in TMPDIR/wverifydir in which it creates,
writes, and reads 100 files.  It recreates and writes the files randomly
and then reads them looking for data corruption.  It varies the buffer
size used for the reads and writes as it goes.

